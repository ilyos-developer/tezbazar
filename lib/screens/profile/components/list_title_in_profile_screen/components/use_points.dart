import 'package:flutter/material.dart';

class UsePoints extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Как использовать поинты"),
      ),
      body: Container(),
    );
  }
}
